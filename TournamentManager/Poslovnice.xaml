<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:TournamentManager.ViewModels"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" x:Class="TournamentManager.Poslovnice"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Poslovnice">

    <Grid Background="{StaticResource PrimaryBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="0.4*"/>
            <RowDefinition Height="8.7*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Column="0" Grid.Row="0" BorderBrush="{StaticResource PrimaryFontColor}" 

                CornerRadius="5"
                Padding="1"
                Margin="10, 15, 10, 0"
                Width="150"
                Background="{StaticResource ButtonBackgroundColor}"
                HorizontalAlignment="Left"    
                VerticalAlignment="Top">

            <Label FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Poslovnice" 
                        TextDecorations="Underline"
                        FontWeight="Bold" 
                        Foreground="{StaticResource PrimaryFontColor}" />
            </Label>
        </Border>

        <Border Grid.Column="2" BorderBrush="{StaticResource PrimaryFontColor}" 
                BorderThickness="2" 
                CornerRadius="5"
                Padding="1"
                Margin="10, 15, 10, 0"
                Width="240"
                Background="{StaticResource ButtonBackgroundColor}"
                HorizontalAlignment="Right"    
                VerticalAlignment="Top"
                MouseLeftButtonDown="DodajPoslovnicu_Click">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.8" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Image Source="/Icons/plus.png" Width="24" Height="24" Margin="0,0,5,0"/>
                <TextBlock Text="Dodaj poslovnicu" VerticalAlignment="Center" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryFontColor}"/>
            </StackPanel>

        </Border>


        <Separator Grid.Row="1" Grid.ColumnSpan="3" Margin="10,10,10,10" Background="{StaticResource PrimaryFontColor}" Height="15"/>


        <!-- Poslovnice i drzave content -->
        <!-- Drzave list -->
        <Grid Grid.Row="2"  Margin="1, 0, 0, 1" Grid.ColumnSpan="3">
            <ListBox ItemsSource="{Binding DrzaveToDisplay}" Background="Transparent" BorderThickness="0" VerticalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Grid.ColumnSpan="3">
                            <TextBlock Text="{Binding CountryName}" Margin="5" FontSize="24" Foreground="{StaticResource PrimaryFontColor}"/>

                            <Separator Margin="10,15,10,10"
                                Background="{StaticResource PrimaryFontColor}"
                                Height="10"/>

                            <!-- Poslovnice po drzavi -->
                            <ItemsControl ItemsSource="{Binding Poslovnice}">

                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{StaticResource PrimaryFontColor}"
                                             Background="{StaticResource ButtonBackgroundColor}"
                                             BorderThickness="1"
                                             CornerRadius="3"
                                             Padding="10"
                                             Margin="5">
                                            <Border.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="Edit" Click="EditOffice_Click"/>
                                                    <MenuItem Header="Delete" Click="DeleteOffice_Click"/>
                                                </ContextMenu>
                                            </Border.ContextMenu>

                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}"  Foreground="{StaticResource PrimaryFontColor}"/>
                                            </StackPanel>

                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                        </StackPanel>
                        
                    </DataTemplate>
                    
                </ListBox.ItemTemplate>
                
            </ListBox>
        </Grid>
        
    </Grid>
</Page>
